const {displayMenu, handleMenuSelection} = require('./userInterface');

// Entry point of the application
function main() {
    displayMenu();
    handleMenuSelection();
    console.log("sarah")
}

main();
